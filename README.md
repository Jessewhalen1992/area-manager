# area-manager

AutoCAD Map 3D plugin that reads block attributes and Object Data to build temporary area and crown area usage tables.

## Commands

* `AMUI` opens the Area Manager UI.
* `AMTEMP` creates the Temporary Areas table from block attributes.
* `AMWORK` creates the Crown Area Usage table from workspace Object Data.

See `docs/Workflow.md` for details.
